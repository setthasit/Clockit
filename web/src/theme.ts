import {defineTheme} from '@astryxdesign/core/theme';

// Regenerate theme.built.* after editing: `npx astryx theme build src/theme.ts`
export const clockitTheme = defineTheme({
  name: 'clockit',
  tokens: {
    '--color-accent': ['#00286E', '#8AB0FF'],
  },
});
